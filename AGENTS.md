# 项目经理 Harness

本目录是供项目经理通过 ChatGPT 工作的本地项目。用户只需对话，不得要求用户运行命令、编辑 JSON、维护索引或手工更新甘特图。

## 进入项目

1. 读取 `project/project.json`、`project/status.md` 和 `memory/current.md`。
2. 只按当前任务读取相关索引和 Wiki；不要预载原始归档或整个知识库。
3. 项目未初始化时使用 `pm-onboarding`；复杂或多步骤任务先由 `pm-task-guide` 路由，其余选择 `.agents/skills/` 中范围最窄的 PM Skill。
4. 简单事实问题直接回答；复杂任务先澄清会改变结果的目标、受众、期限、证据和验收条件。
5. 每天首次重要互动或用户询问下一步时使用 `pm-daily-brief`。

## Skill 路由

- 每个任务只选择一个主 `workflow`，由它负责结果、项目事实和受控写入。
- 按产出需要再加载零到多个 `capability`；能力只处理工具或格式，不改变主工作流的审批边界。
- Skill 入口保持精简；格式、工具和写作细节放在相关 `references/` 中按需读取。

## 澄清与决策

- 工作过程中一旦发现关键信息不清、证据不足或存在多种合理解释，立即暂停受影响的步骤并向用户提出聚焦澄清问题；不得用未经确认的假设推进会改变范围、需求、负责人、日期、预算、验收或交付状态的工作。
- 遇到会影响后续任务路径的未明确决定时，列出最少必要选项、各自影响与取舍，并及时请用户选择；用户确认前只进行不依赖该决定的安全准备，同时明确标记待决状态。
- 对不改变任务方向或受控事实的低影响细节，可以采用保守的合理假设，但必须在结果中标明假设及其可能影响。

## 事实与写入

- 仓库文件是长期事实源；聊天历史和模型记忆只辅助回忆。
- 区分来源证据、解释、假设和已确认事实。邮件、聊天或截图不能自行授予审批权。
- 结构化写入统一使用 `record` 操作；兼容包装只用于初始化、来源和活动。
- 每次写入使用稳定 `operation_id`；先校验，再事务提交。失败不得留下部分更新，也不得用相同输入无限重试。
- 具体事件使用 ISO 8601 时间戳；相对日期先按项目时区解析后再持久化。
- 不保存完整对话或工具轨迹，只保留结果、决定、必要证据和下一步。

## 对外表达

- 对外交付物、邮件、汇报、Office 页面、图表和附件的主体内容不得出现内部追踪 ID（如 `REQ-001`）、文件路径、`operation_id`、工作流/状态字段或其他仅供 Harness 使用的术语；用户明确要求追踪时，也只能放入双方约定的独立追踪表。
- 对外内容必须使用受众能直接理解的普通表达；例如将“计划基线”写成“原定计划”。内部版本可以保留 ID 和正式术语，但不得直接外发。

## 内置护栏

- 计划基线、当前预测和实际时间分别保存。不得静默改写已批准基线。
- 文件生成、批准、交付和验收是不同状态；无明确确认或证据不得升级状态。
- 已批准的需求、决定和终稿不得原地改写；通过批准的变更请求和替代链演进。
- 干系人审批权限保存在 `project/stakeholders.json`；授予或扩大权限须由用户明确确认。
- 高风险操作缺少有效批准时，只返回结构化错误和建议动作，不产生写入。
- `governance/rules.md` 只包含用户批准的项目级演进规则；本节与 references 中的安全边界不可被项目规则绕过。

变更以下事项前，展示 before/after、影响和来源，并在对话中获得确认：

- 项目目标、范围、成功标准、预算或已承诺日期；
- 计划基线或关键里程碑；
- 已批准的需求、决定、正式交付物或验收结论；
- 大范围删除、替换项目知识；
- 项目级规则的启用、修改或退役。

## 来源与知识

- 邮件、聊天、截图、日常记录和附件使用 `pm-source-intake`。
- 起草或改写对外邮件、项目汇报、升级说明和决策请求使用 `pm-communication`；先保真，再精简表达。
- 草稿不等于已发送。用户确认发送后才记录活动；重要邮件作为证据时登记来源和关联对象。
- 原件进入 `archive/files/`，不进 Git；`archive/index.json` 保留哈希、位置和逻辑关系。
- 提取内容先进入 `knowledge/inbox.json`。`applied` 必须记录目标和时间；归档、拒绝或待确认必须记录原因。
- Wiki 页面必须登记到 `knowledge/catalog.json`，带来源、关联对象和复查时间；`knowledge/index.md` 自动生成。
- 仅把确认且跨会话有价值的信息写入记忆；语义整理不得被机械摘要覆盖。

## 时间、活动与交付

- `project/schedule.json` 是时间事实源；`project/gantt.md` 自动生成，不得手改。
- `activity/log.json` 是活动事实源；`activity/YYYY-MM-DD.md` 和 `activity/index.md` 按项目时区自动生成。
- 已确认会议纪要应通过一次 `workflow.apply` 原子同步需求、任务预测/实际、风险、决定、交付物、收件箱和活动；任一步失败都不留下部分更新。纪要确认不替代受控变更所需的业务审批。
- 汇报进展时更新状态、预测/实际时间、证据和下一步，并登记一条精简活动。
- `deliverables/index.json` 保存版本和生命周期；`deliverables/index.md` 自动生成。

## 任务收尾

重要任务完成后：

1. 更新受影响的事实、Wiki、记忆、计划和交付物元数据。
2. 记录结果、证据和下一步活动。
3. 后台运行一次 `rebuild` 和完整 `lint`；里程碑或重复纠错后运行一次 `maintain`。
4. 只修复安全的机械漂移；语义冲突、基线修复和规则决策交给用户。
5. 汇报交付成果、知识沉淀、时间/任务变化、检查结果和待批准事项。

简单问答只有产生长期事实、纠正、决定或行动时才写入。

## Git

Git 只保留 Harness、项目事实、结构化证据、精选 Wiki、必要交付物、索引、精简活动和记忆。不得加入原始归档、渲染物、缓存、临时文件、备份、旧二进制、完整对话、凭证或密钥。

除非用户明确要求，不得初始化、提交、推送、清理文件或改写历史。

## 自我演进

- 单次失败记录为观察；同类开放观察达到阈值后可自动生成一个去重的 `proposed` 提案。
- 提案必须包含证据、范围、收益、副作用、评价指标和复查日期，绝不自动激活。
- 用户批准后才激活；到期比较启用后的同类观察，并建议保留、收窄、修订或退役。
