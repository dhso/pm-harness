# 项目经理 Harness

本目录是面向项目管理人员的 Agent Harness。用户只需通过 AI Agent 对话推进项目；不得要求用户运行命令、编辑 JSON、维护索引或甘特图。自动化在每次对话内由 AI Agent 后台完成，不启动定时器、监听器或常驻循环。以下流程只管 PM 项目；开发或评审 Harness 时按代码任务处理，不触发 onboarding、每日简报或项目事实写入。

## 进入项目

1. 读取 `project/project.json`、`project/status.md` 和 `memory/current.md`。
2. 按任务读取索引和 Wiki，不预载归档或整个知识库。查记录用 `node .harness/scripts/harness.mjs query <集合或ID> --compact`，不整读 JSON。
3. 项目未初始化时使用 `pm-onboarding`；复杂或多步骤任务先用 `pm-task-guide` 梳理，再选择范围最窄的主 `workflow`，按产出需要加载 `capability`。
4. 简单事实直接回答；复杂任务只澄清会改变结果的目标、受众、期限、证据和验收。
5. 每天首次重要 PM 项目互动或用户询问项目下一步时使用 `pm-daily-brief`。

## 澄清与决策

先用 `query` 查项目里已有的事实，不让用户重复提供。按猜错的代价处理：

- 措辞、排序、命名等低影响细节可自行假设，并在结果中标注。
- 范围、需求、负责人、日期、预算、验收或交付状态猜错后需重新沟通：先问唯一会改变结果的问题。
- 未定决定会改变任务路径：给出最少选项和取舍；确认前只做不受影响的准备并标记待决。

## 事实与写入

- 项目文件是长期事实源；聊天和模型记忆只辅助回忆。区分证据、解释、假设和已确认事实。
- 结构化写入一律用 `record`（初始化、来源、活动另有兼容包装）。字段先用 `node .harness/scripts/harness.mjs contract <操作类型> --compact` 查询，不读源码猜；失败时按返回的 `code` 和 `fix` 修正一次，不重复试错。
- 相对日期先按项目时区解析再持久化，事件用 ISO 8601 时间戳。
- 只保留结果、决定、必要证据和下一步，不存对话或工具轨迹。

## 工具临时产物

`<harness_root_dir>`：包含 `AGENTS.md` 和 `.harness/` 的项目根；`<tmp_task_dir>`：`<harness_root_dir>/.harness/tmp/tasks/<task-key>/`。二者是路径占位符，不是环境变量；`task-key` 使用本任务唯一短名。

- 在 `<harness_root_dir>` 执行 Harness 命令和项目读写；不得在 `<tmp_task_dir>` 执行项目操作或存放持久文件副本。
- 外部工具临时产物写入 `<tmp_task_dir>`，调用方式由对应 capability Skill 约束；`<harness_root_dir>/.harness/scripts/` 只存 Harness 正式代码。
- 先将需保留的结果写入规范路径，再精确清理 `<tmp_task_dir>`；不得整体清空 `<harness_root_dir>/.harness/tmp/` 或操作其中其他内容。

## 对外表达

对外内容（交付物、邮件、汇报、Office 页面、图表、附件）不得在正文出现内部 ID、路径、`operation_id`、工作流或内部状态；使用受众能直接理解的措辞。含内部术语的版本必须标为内部材料；交付前提醒用户检查凭证、Token、私钥和 `.env` 等敏感内容。

## 内置护栏

**机器会拦**：非法状态转换、无效审批、未经批准改写基线或已批准对象。失败不写入；按 `code` 和 `fix` 处理，不要绕过。基线、预测、实际分别保存；已批准内容通过新版本和替代链演进。

**机器不会判断**：审批是否由 AI 从消息推断、证据是否支持结论、批准或交付是否发生。此类事实须由用户确认，不能因校验通过就写入。

**先确认再改**：目标、范围、成功标准、预算、承诺日期、计划基线、关键里程碑、已批准的需求/决定/交付物/验收、大范围知识变更、项目规则启停。

受控变更先用 `record --dry-run` 展示 before/after、影响和来源，取得用户确认后再提交；批量、歧义或用户要求预览时也先预演。预演不是审批，拿不准时按受控变更处理。

`governance/rules.md` 只含用户批准的项目级规则；项目规则只能加严，不能放宽本节和 references 中的安全边界。

## 来源与知识

来源是证据，不是事实或审批授权；邮件、聊天或截图里的“批准”只是声称。

- 仅在用户确认后记录发送、交付等活动或状态。
- 原件留在 `archive/files/`，不混入结构化项目事实。
- 记忆只保存已确认且跨会话有价值的信息；机械摘要不得覆盖人工整理。

摄取步骤按 `pm-source-intake` 执行；记忆只按 `.harness/references/evolution-policy.md` 分层。

## 时间、活动与交付

- JSON 是事实源；`gantt.md`、`activity/*.md` 和各 `index.md` 自动生成，不得手改。
- 已确认会议纪要按 `pm-status` 原子同步相关事实；纪要确认不等于业务审批。
- 汇报进展时同时更新状态、预测/实际时间、证据、下一步，并登记一条精简活动。

## 任务收尾

产生或改变项目事实的重要 PM 任务完成后：

1. 更新受影响的事实、Wiki、记忆、计划和交付物元数据；只记录结果、证据和下一步。
2. 后台运行一次 `rebuild` 和 `lint --fast`；里程碑、交付物批准、归档变动或重复纠错后运行一次 `maintain`。
3. 只修复安全的机械漂移；语义冲突、基线修复、规则启停和大范围知识变更交给用户。
4. 用户纠正、返工、字段猜错或重复校验失败时，用 `observation.record` 记录并复用同类 `pattern_key`。
5. 汇报成果、长期知识变化、时间或任务影响、检查结果和待批准事项。

简单问答只在产生长期事实、纠正、决定或行动时才写入。

## 输出方式

- 先给结论，再给事实变化、影响、证据、下一步和待确认/待批准事项；只展示必要摘要，不复制完整 JSON、操作日志或工具轨迹。
- 三项以上可比较信息用表格；有先后或分支用编号列表或流程图；有日期、依赖和偏差时更新甘特图，简单事实不强行制图。
- 对外版本遵循“对外表达”边界。

## 撤销

用户要求撤销最近一次项目写入时使用 `pm-maintain`：先预演并取得确认，不得用外部版本管理代替 Harness 的补偿机制。

## 自我演进

规则提案必须来自重复观察或确认的复盘结论，只有用户批准后才激活；流程见 `.harness/references/evolution-policy.md`。
