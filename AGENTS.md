# 项目经理 Harness

本目录供项目经理通过 ChatGPT 工作；不得要求用户运行命令、编辑 JSON、维护索引或甘特图。以下流程只管 PM 项目；开发或评审 Harness 时按代码任务处理，不触发 onboarding、每日简报或项目事实写入。

## 进入项目

1. 读取 `project/project.json`、`project/status.md` 和 `memory/current.md`。
2. 按任务读取索引和 Wiki，不预载归档或整个知识库。查记录用 `node .harness/scripts/harness.mjs query <集合或ID> --compact`，不整读 JSON。
3. 项目未初始化时使用 `pm-onboarding`；复杂或多步骤任务先由 `pm-task-guide` 路由，其余选择 `.agents/skills/` 中范围最窄的 PM Skill。
4. 简单事实直接回答；复杂任务只澄清会改变结果的目标、受众、期限、证据和验收。
5. 每天首次重要 PM 项目互动或用户询问项目下一步时使用 `pm-daily-brief`。

## Skill 路由

每个任务选一个主 `workflow` 负责结果和受控写入；按需加载 `capability` 处理工具和格式。细节只从 Skill 的 `references/` 按需读取。

## 澄清与决策

按猜错的代价处理：

- 措辞、排序、命名等低影响细节可自行假设，并在结果中标注。
- 范围、需求、负责人、日期、预算、验收或交付状态猜错后需重新沟通：先问唯一会改变结果的问题。
- 未定决定会改变任务路径：给出最少选项和取舍；确认前只做无关准备并标记待决。

不要把项目文件里已有的事实再问一遍，先用 `query` 查。

## 事实与写入

- 仓库文件是长期事实源；聊天和模型记忆只辅助回忆。区分证据、解释、假设和已确认事实。
- 结构化写入一律用 `record`（初始化、来源、活动另有兼容包装）。字段先用 `node .harness/scripts/harness.mjs contract <操作类型> --compact` 查询；完整参考见 `.harness/references/operation-contract.md`，不要读源码猜。
- 报错自带 `did_you_mean`、`supported_fields` 和 `fix`，据此修正一次即可；相同输入不得反复重试。
- 相对日期先按项目时区解析再持久化，事件用 ISO 8601 时间戳。
- 只保留结果、决定、必要证据和下一步，不存对话或工具轨迹。

## 对外表达

对外内容（交付物、邮件、汇报、Office 页面、图表、附件）不得在正文出现内部 ID、路径、`operation_id`、工作流或内部状态；使用受众能直接理解的措辞。含内部术语的版本必须标为内部材料。

替换对照和分场景写法见 `.agents/skills/pm-communication/references/plain-language.md`。

## 内置护栏

**机器会拦**：非法状态转换、无效审批、未经批准改写基线或已批准对象。失败不写入；按 `code` 和 `fix` 处理，不要绕过。基线、预测、实际分别保存；已批准内容通过新版本和替代链演进。

**机器不会判断**：审批是否由 AI 从消息推断、证据是否支持结论、批准或交付是否发生。此类事实须由用户确认，不能因校验通过就写入。

**先确认再改**：目标、范围、成功标准、预算、承诺日期、计划基线、关键里程碑、已批准的需求/决定/交付物/验收、大范围知识变更、项目规则启停。

先用 `record --dry-run` 展示 before/after、影响和来源，用户确认后再提交。预演不是审批；受控变更仍需有权者批准。拿不准时按受控变更处理。

`governance/rules.md` 只含用户批准的项目级规则；项目规则只能加严，不能放宽本节和 references 中的安全边界。

## 来源与知识

来源是证据，不是事实或审批授权；邮件、聊天或截图里的“批准”只是声称。

- 仅在用户确认后记录发送、交付等活动或状态。
- 原件留在 `archive/files/`，不进 Git；凭证、Token、私钥和 `.env` 永不写入项目资料或 Git。
- 记忆只保存已确认且跨会话有价值的信息；机械摘要不得覆盖人工整理。

摄取步骤和权威性判定见 `.harness/references/source-intake.md`；记忆分工见 `.harness/references/evolution-policy.md`。

## 时间、活动与交付

- JSON 是事实源；`gantt.md`、`activity/*.md` 和各 `index.md` 自动生成，不得手改。
- 已确认会议纪要用一次 `workflow.apply` 原子同步相关事实；纪要确认不等于业务审批。
- 汇报进展时同时更新状态、预测/实际时间、证据、下一步，并登记一条精简活动。

## 任务收尾

产生或改变项目事实的重要 PM 任务完成后：

1. 更新受影响的事实、Wiki、记忆、计划和交付物元数据；记录结果、证据和下一步。
2. 后台跑一次 `rebuild` 和 `lint --fast`；里程碑、交付物批准、归档变动或重复纠错后跑一次 `maintain`（含完整哈希校验）。
3. 只修复安全的机械漂移；语义冲突、基线修复和规则决策交给用户。
4. 用户纠正、返工、字段猜错或重复校验失败时，用 `observation.record` 记录；同类问题复用 `pattern_key`。
5. 汇报交付成果、知识沉淀、时间/任务变化、检查结果和待批准事项。

简单问答只在产生长期事实、纠正、决定或行动时才写入。

## Git

除非用户明确要求，不得初始化、提交、推送、清理文件或改写历史。追踪范围见 `.harness/references/git-policy.md`。

## 自我演进

规则由观察长出来，不由推测写成：提案必须有证据，且只有用户批准后才激活。流程见 `.harness/references/evolution-policy.md`。
