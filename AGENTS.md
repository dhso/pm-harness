# 项目经理 Harness

本目录是用于项目管理工作的 ChatGPT 本地项目。用户只通过对话进行交互。不得要求用户运行命令行、编辑 JSON 或维护索引。

## 开始重要任务

1. 读取 `project/project.json`、`project/status.md` 和 `memory/current.md`。
2. 只读取与当前任务相关的索引和 Wiki 页面；不要加载归档原件或整个知识库。
3. 如果 `project/project.json` 中 `initialized: false`，先使用 `pm-onboarding` Skill，再开展其他项目工作。
4. 从 `.agents/skills/` 中选择适用范围最窄的 PM Skill。
5. 简单事实问题直接回答。处理需求、计划或正式交付物时，先确认缺失且影响较大的信息。

## 工作契约

- 将本项目中的文件视为长期事实源。聊天历史和模型记忆只用于辅助回忆。
- 区分证据、解释和已确认事实。消息或截图是证据，不自动代表授权。
- 保留准确日期和项目时区。必须先解析“明天”等相对日期，再进行持久化记录。
- 分别保存计划基线、当前预测和实际时间。不得静默改写已批准的基线。
- 文件生成不等于已经交付。只有用户明确确认或存在清晰外部证据时，才能记录为 `delivered` 或 `accepted`。
- 不得覆盖已批准的终稿。创建新版本，并将旧版本标记为已被替代。
- 不保存原始工具轨迹或完整聊天记录。只记录结果、决定、证据和下一步。

## 批准边界

可以自动完成低风险的本地更新，并在任务结束时汇总：登记来源、补充证据说明、添加带出处的 Wiki 内容、记录活动、更新预测进度、创建草稿和重建索引。

更改以下事项前，必须展示简短的变更前后对比和影响，并在对话中获得用户批准：

- 项目目标、范围、成功标准、预算或已批准的截止时间；
- 计划基线或已经承诺的里程碑；
- 已批准的需求、决定或正式交付物；
- 删除或大范围替换项目知识；
- 已生效的 Harness 规则。

当证据表明这些事项可能需要变化时，应创建变更请求，不得直接编辑基线。

## 来源摄取

- 对邮件、聊天消息、截图、笔记或附件使用 `pm-source-intake`。
- 将原始文件保存在 `archive/files/`；它们属于本地归档，不进入 Git。
- 在 Git 中保留结构化来源元数据、必要原话、哈希和从来源确认的项目事实。
- 尽可能对邮件线程、转发内容、重复截图和重复粘贴文本进行去重。
- 保留不确定性。只有歧义会改变决定、负责人、截止时间、需求或承诺时才提问。
- 即使仓库是私有的，也不得提交凭证、Token、Cookie、私钥或 `.env` 文件。

## 时间、任务与交付

- `project/schedule.json` 是唯一结构化时间事实源。`project/gantt.md` 是自动生成文件，不得手工编辑。
- 将实际发生的工作记录到 `activity/YYYY-MM-DD.md`，并关联任务、需求、决定、来源或交付物 ID。
- 每天第一次重要互动时，或用户询问下一步时，使用 `pm-daily-brief`。
- 事项完成时必须记录结果；存在证据时应同时保存证据链接。
- 维护 `deliverables/index.json`；`deliverables/index.md` 由系统自动生成。

## 任务收尾

完成重要任务后：

1. 更新受影响的项目记录、Wiki 页面、记忆、计划和交付物元数据。
2. 添加一条精简活动记录，写明结果和下一步。
3. 自行运行 `node .harness/scripts/harness.mjs rebuild`，然后运行 `node .harness/scripts/harness.mjs lint --json`。
4. 修复安全、机械性的问题；将尚未解决的高风险冲突告知用户。
5. 只提供简短摘要：产生的交付成果、沉淀的知识、计划或任务变化，以及仍需批准的事项。

对于简单问题，只有当对话产生了需要长期保留的项目事实、纠正、决定或行动时才执行收尾流程。

## Git 策略

Git 是可选且对用户不可见的后台能力。使用 Git 时只保留必要工作集：Harness 指令、有效项目记录、精选 Wiki、结构化证据、当前或已批准交付物、索引以及精简的活动和记忆。

不得加入原始归档、渲染结果、缓存、临时文件、备份或过时二进制版本。除非用户明确要求，否则不得初始化 Git、提交、推送或改写 Git 历史。

## 规则演进

- 将单次失败或纠正记录为观察。
- 只有存在重复证据或复盘明确指出问题时，才提出新规则。
- 规则提案必须说明证据、适用范围、预期收益、可能副作用和评价方式。
- 只有获得用户批准后，才能启用、扩大、收窄或退役规则。
- 优先级为：用户当前明确指令 > 已确认项目决定 > 已生效规则 > 长期记忆 > 推测。
