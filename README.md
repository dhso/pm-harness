# PM Harness

一个面向 ChatGPT 桌面端本地项目的项目经理协作模板。

项目经理只需通过自然语言描述目标、提问、提供邮件或截图、汇报进展以及要求制作交付物。ChatGPT 在后台维护项目计划、甘特图、需求、风险、知识库、记忆、活动记录、交付物和索引，并在重要操作后进行一致性检查。

## 适用场景

- 启动和规划一个独立项目；
- 日常跟进任务、时间点、风险和阻塞；
- 从邮件、聊天、截图、附件或随手记录中提取项目事项；
- 起草可直接发送的项目邮件、升级说明、决策请求和会议跟进；
- 编写或变更需求、验收标准和项目范围；
- 制作周报、方案、会议纪要、PRD、汇报材料等交付物；
- 沉淀可追溯的项目 Wiki、决策和经验；
- 在不同 ChatGPT 会话之间延续项目上下文；
- 用 Git 管理必要事实和交付成果，同时排除体积大、重复且不适合版本管理的归档原件。

一个目录对应一个项目，不建议把多个独立项目混在同一份 Harness 中。

## 快速开始

1. 为项目复制一份本目录。
2. 在 ChatGPT 桌面端创建本地项目，并把复制后的目录设为项目目录。
3. 对 ChatGPT 说：`初始化这个项目`。
4. 根据引导提供项目名称、目标、成功标准、范围、干系人、已知时间点和预期交付物。
5. 此后直接用自然语言开展项目工作。

项目经理不需要运行命令、编辑 JSON、维护索引或手工更新甘特图。`.harness/` 中的内部工具只供 ChatGPT 自动维护本地文件。

## 可以怎样和 ChatGPT 协作

### 项目启动与计划

- `初始化这个项目，目标是在 11 月完成新产品内测。`
- `根据已有需求制定项目计划和里程碑。`
- `客户评审延迟了一周，分析影响并更新预测时间。`
- `今天最应该推进哪三件事？`

### 邮件、聊天、截图和日常记录

- `这是一封客户邮件，帮我登记并提取需求、行动项和风险。`
- `把这些聊天截图整理成项目事实、待办和待确认事项。`
- `根据这些记录写会议纪要；我确认后，把行动项、需求、排期和风险同步到项目。`
- `根据当前进展给客户写一封简短但信息完整的延期说明，先不要发送。`
- `把会议结论写成跟进邮件，列清负责人、日期和待确认问题。`
- `这是今天的工作记录，更新进度、甘特图和下一步。`
- `这条消息可能改变验收范围，先分析影响，不要直接修改基线。`

来源材料首先被视为证据，不会自动变成已经批准的决定。发送者、时间、原话、权威性或含义不明确，并且会影响负责人、日期、需求或承诺时，ChatGPT 会提出一个聚焦问题。

工作过程中如果发现关键信息不清、证据不足或存在多种合理解释，ChatGPT 会及时暂停受影响的步骤并向你澄清。对于会影响后续任务路径的未明确决定，会列出必要选项、影响和取舍，请你选择后再继续；不改变方向的低影响细节才会采用并标注保守假设。

### 需求和交付物

- `根据目前资料写一份 PRD，缺少的关键信息再问我。`
- `把这条反馈作为候选需求记录，并给出验收标准。`
- `为管理层制作本周项目周报。`
- `把周报改得更像项目经理写的：结论前置、说人话，但不要丢日期、数字和风险。`
- `根据评审意见修订方案，保留旧版本和变更关系。`
- `客户已经确认验收，请更新交付状态。`
- `用 OfficeCLI 检查这个 Excel 的公式和格式，并生成一份带图表的 PPT。`

文件生成、批准、发送和验收是不同状态。ChatGPT 不会因为文件已经生成就把它记录为已交付或已验收；批准后还会用文件 SHA-256 检测原地覆盖。

## Harness 的工作闭环

```text
对话、邮件、截图、附件、日常记录
                  │
                  ▼
       来源登记、去重和证据提取
                  │
        ┌─────────┼─────────┐
        ▼         ▼         ▼
     项目知识    任务风险    需求/变更提案
        │         │         │
        └─────────┼─────────┘
                  ▼
       计划、甘特图、交付物和活动记录
                  │
                  ▼
       当前记忆、日常引导和一致性检查
                  │
                  ▼
      复盘观察 → 规则提案 → 用户批准
                  │
                  └───────────► 后续工作方式优化
```

文件是长期事实源；ChatGPT 的会话记忆只是辅助。重要信息会被整理进项目文件，而不是依赖某一次聊天上下文。

## 目录总览

```text
pm-harness/
├── AGENTS.md                  ChatGPT 的项目级工作规则
├── README.md                  本使用说明
├── project/                   项目定义、计划、需求、状态和登记册
├── knowledge/                 来源、待处理信息和精选 Wiki
├── memory/                    跨会话工作记忆、事实、偏好和经验
├── activity/                  按日期保存的实际工作记录
├── deliverables/              当前交付物、批准终稿和交付索引
├── governance/                有效规则、规则提案和变更记录
├── archive/                   不进入 Git 的原始资料及其可追溯索引
├── templates/                 常用项目管理文档模板
├── .agents/skills/            实体、可跨平台复制的项目管理 Skills
├── .harness/                  后台自动化、数据契约和检查规则
├── tests/                     Harness 自动回归测试
├── .gitignore                 Git 排除策略
├── .gitattributes             二进制文件属性
└── package.json               后台工具的运行配置
```

## `project/`：项目事实与计划

| 路径 | 作用 | 维护方式 |
|---|---|---|
| `project/project.json` | 项目名称、目标、范围、成功标准、约束、状态和时区 | 初始化时建立；重大变化需先确认 |
| `project/stakeholders.json` | `STK-###` 干系人、角色、组织、状态、审批范围、来源和更新时间 | 权限授予或扩大前必须由用户确认 |
| `project/status.md` | 给人阅读的当前状态摘要，包括重点、阻塞和下一步 | 进展或状态显著变化后更新 |
| `project/schedule.json` | 任务、里程碑、负责人、依赖、进度以及基线/预测/实际时间的唯一结构化事实源 | ChatGPT 根据计划和进展维护 |
| `project/gantt.md` | 从时间计划自动生成的 Mermaid 甘特图和时间偏差表 | 自动生成，不应手工编辑 |
| `project/requirements.json` | 需求和变更请求，包括状态、验收标准、来源和影响 | 需求工作或变更控制时维护 |
| `project/registers.json` | 风险、问题和决策登记册 | 状态汇报、会议或来源摄取后维护 |

计划时间分为三类：

- `baseline_*`：已经批准的原始基线；未经确认不得修改。
- `forecast_*`：根据当前进展做出的最新预测；用于显示提前或延期。
- `actual_*`：实际开始和完成时间；用于复盘和交付证明。

`schedule.baseline` 同时保存 revision、当前日期字段的 digest、批准人、批准时间和关联变更请求。任何直接改动都会造成摘要或批准日志不一致并被检查阻止。因此，延期会表现为预测相对基线的偏差，而不是通过移动基线被隐藏。

## `knowledge/`：来源和 Wiki

| 路径 | 作用 | 是否进入 Git |
|---|---|---|
| `knowledge/sources.json` | 邮件、聊天、截图、文档等来源的结构化登记，保存来源 ID、摘要、时间、发送者、定位信息和哈希 | 是 |
| `knowledge/inbox.json` | 从来源中提取、尚待处理的事实、反馈、请求、决定、承诺、行动、风险、问题、疑问和假设 | 是 |
| `knowledge/catalog.json` | `WIKI-###`、页面路径、状态、来源、关联对象与复查日期的事实源 | 是 |
| `knowledge/wiki/` | 按主题整理并带来源追溯的长期项目知识 | 是 |
| `knowledge/index.md` | Wiki 页面导航 | 是；自动生成 |

典型处理过程是：

1. 为材料分配 `SRC-###` 来源 ID。
2. 尽可能根据文件哈希、消息定位信息或邮件线程信息去重。
3. 将有用内容拆分为独立的 `INB-###` 条目。
4. 区分可确认事实、解释、假设和待确认问题。
5. 将低风险且可靠的信息写入任务或 Wiki。
6. 涉及范围、需求、预算、承诺日期或验收变化时创建 `CR-###`，等待确认后再应用。

Wiki 只保存整理后的项目知识和必要引用，不复制整封邮件、完整聊天记录或大量截图文字。

## `memory/`：跨会话记忆

| 路径 | 记录内容 |
|---|---|
| `memory/current.md` | 当前重点、阻塞、未决问题和下一步；保持短小并持续刷新 |
| `memory/facts.md` | 已确认、跨会话仍有价值的项目事实 |
| `memory/preferences.md` | 你明确确认或反复体现的协作、写作和交付偏好 |
| `memory/lessons.md` | 经复盘确认、适合在本项目复用的经验 |
| `memory/observations.json` | 单次错误、纠正、异常和改进信号，是规则演进的证据池 |

不会把猜测、临时讨论、一次性措辞或完整对话直接固化为长期记忆。信息发生冲突时保留证据并请求确认，不用新内容静默覆盖旧事实。

## `activity/`：实际发生过的工作

| 路径 | 作用 |
|---|---|
| `activity/log.json` | `ACT-###` 活动、准确时间、结果、关联 ID、证据和下一步的唯一事实源 |
| `activity/index.md` | 自动生成的日期索引 |
| `activity/YYYY-MM-DD.md` | 按项目时区自动生成的当日可读视图，不应手工编辑 |

活动记录回答“什么时间做了什么、产生了什么结果”，用于日常追踪、周报、复盘和计划校准。它不保存完整聊天或工具运行日志，避免制造无价值噪声。

## `deliverables/`：交付物生命周期

| 路径 | 作用 | 是否进入 Git |
|---|---|---|
| `deliverables/current/` | 当前正在编写、评审或使用的交付物 | 是 |
| `deliverables/final/` | 已批准且仍需保留的正式终稿 | 是 |
| `deliverables/.render/` | Office 文件视觉检查产生的临时图片或预览 | 否 |
| `deliverables/index.json` | 交付物 ID、版本、状态、受众、用途、批准文件哈希、时间、验收和替代关系的事实源 | 是 |
| `deliverables/index.md` | 给人阅读的交付物状态表 | 是；自动生成 |

标准状态为：

```text
requested → drafting → review → approved → delivered → accepted
```

交付物也可以进入 `superseded` 或 `cancelled`。已批准文件不得原地覆盖；正式修订会建立新版本和新的 `DEL-###`，并通过替代关系保留版本链。不再需要 Git 跟踪的旧版本移入本地归档。

## `governance/`：规则与受控演进

| 路径 | 作用 |
|---|---|
| `governance/rules.json` | 已批准项目规则的结构化事实源、评价指标关联和复查时间 |
| `governance/rules.md` | 当前生效项目规则的自动生成视图，不包含内置安全护栏 |
| `governance/proposals.json` | 有证据支持但尚未决定的规则改进提案 |
| `governance/change-log.json` | `CHG-###` 受控变更与幂等操作记录，保存 before/after 摘要及哈希、批准人和生效时间 |

一次错误只会形成 `OBS-###` 观察。同类开放问题达到阈值，或复盘明确指出流程缺陷后，统一维护流程才生成一份去重的 `RULE-###` 提案。每项提案必须说明证据、适用范围、收益、副作用、评价指标和复查时间。

规则不会自动生效。只有你明确批准后，ChatGPT 才会更新有效规则，并在后续复查它是否应该保留、收窄、修订或退役。

## `archive/`：本地原始资料归档

| 路径 | 作用 | 是否进入 Git |
|---|---|---|
| `archive/files/` | 原始邮件、聊天导出、截图、附件、历史草稿和旧版二进制文件 | 否 |
| `archive/index.json` | 归档记录 ID、逻辑对象、文件名、类型、SHA-256、归档时间、原因、替代关系和可用状态 | 是 |

每个归档条目使用 `ARC-###`。即使原件没有进入 Git，仍可从结构化项目记录追溯到归档索引，并在本机存在原件时进行核验。

原始资料被排除主要是为了控制仓库体积、重复内容和二进制噪声，而不是因为私有仓库不能保存正常项目内容。凭证、Token、Cookie、验证码、私钥和 `.env` 文件仍不应进入项目资料或 Git 历史。

## `templates/`：交付模板

| 模板 | 用途 |
|---|---|
| `project-brief.md` | 项目概述、目标、范围和成功标准 |
| `project-plan.md` | 项目阶段、里程碑、依赖和治理计划 |
| `prd.md` | 产品或项目需求文档 |
| `deliverable-brief.md` | 制作正式交付物前明确目的、受众和验收方式 |
| `project-email.md` | 项目通知、请求、跟进、升级和交付邮件 |
| `status-report.md` | 周报、阶段状态或管理层汇报 |
| `executive-brief.md` | 面向管理层的精简决策材料 |
| `meeting-notes.md` | 会议讨论、决定、行动项、项目影响和后续同步清单 |
| `decision-record.md` | 记录选项、依据、批准人和决定结果 |
| `change-request.md` | 范围、需求、时间或验收变化的 before/after 和影响分析 |
| `risk-register.md` | 风险、概率、影响、责任人和应对措施 |
| `stakeholder-register.md` | 干系人角色、影响力、关注点和沟通方式 |
| `communication-plan.md` | 沟通对象、内容、频率、渠道和责任人 |
| `source-note.md` | 对来源材料进行摘要、引用和可信度整理 |
| `acceptance-report.md` | 对照验收标准记录证据和结论 |
| `retrospective.md` | 复盘目标、偏差、经验和后续行动 |

模板是内容脚手架，不要求机械填满所有字段。ChatGPT 会根据实际受众和用途进行调整。

## `.agents/skills/`：场景化工作能力

| Skill | 触发场景 |
|---|---|
| `pm-onboarding` | 初始化一份尚未建立项目事实的 Harness |
| `pm-task-guide` | 在复杂或模糊任务开始前明确结果、受众、约束和验收方式 |
| `pm-daily-brief` | 每天首次重要互动或询问下一步时生成聚焦工作建议 |
| `pm-source-intake` | 处理邮件、聊天、截图、日常记录和附件 |
| `pm-knowledge` | 回答项目问题或整理 Wiki 知识 |
| `pm-requirements` | 编写、完善或变更需求和验收标准 |
| `pm-planning` | 创建或调整计划、里程碑、依赖和时间预测 |
| `pm-status` | 编写会议纪要，并将已确认内容同步到需求、排期、风险、决定和状态 |
| `pm-communication` | 起草或改写简练、自然且不丢事实的项目邮件和汇报 |
| `pm-deliverable` | 创建、评审、版本化和整理正式交付物 |
| `officecli` | 使用 OfficeCLI 读取、检查、创建和修改 Word、Excel、PowerPoint 文件 |
| `pm-review` | 评审交付成果、执行验收或开展复盘 |
| `pm-maintain` | 重建索引、检查数据一致性、整理记忆并提出规则改进 |

`.agents/skills/` 直接保存实际 Skill 内容，不使用软链接，也没有需要同步维护的根 `skills/` 副本。复制目录、下载压缩包或在 Windows 上使用时都能保持一致。

Skill 采用“一个主工作流 + 可选能力适配器”的组合方式。`pm-task-guide` 先完成任务分类，再选择一个负责项目结果和受控写入的 `workflow`，按需附加 `capability`（例如 `officecli`）。Skill 的 frontmatter 使用 `kind`、`domain`、`owner` 和 `composes` 声明职责关系；详细格式规则只在任务需要时读取 `references/`，避免一次加载整套手册。

## `.harness/`：后台自动维护能力

该目录面向 ChatGPT，而不是项目经理。

| 路径 | 作用 |
|---|---|
| `.harness/config.json` | 时区、近期窗口、任务陈旧阈值、重复观察阈值和归档目录等配置 |
| `.harness/references/` | 来源摄取、计划、变更、交付、Git 和规则演进的详细政策 |
| `.harness/lib/core.mjs` | 稳定公共 API 门面；CLI 和测试只需从这里导入 |
| `.harness/lib/workspace.mjs` | 工作区数据文件、集合映射和受控字段定义 |
| `.harness/lib/model.mjs` | 状态、字段、日期、ID、引用、状态转换和摘要哈希的唯一可执行契约 |
| `.harness/lib/contracts.mjs` | 统一写入信封、类型化 payload、嵌套记录和工作流步骤的封闭字段契约 |
| `.harness/lib/helpers.mjs` | 日期、路径、哈希、数组和记录处理等无状态公共函数 |
| `.harness/lib/approval.mjs` | 审批范围校验、用户确认和受控变更日志 |
| `.harness/lib/operations.mjs` | 类型化 `record`、精确变更绑定和轻量原子工作流 |
| `.harness/lib/content-operations.mjs` | 收件箱、交付物、Wiki、观察和规则等内容域写入 |
| `.harness/lib/compat.mjs` | 初始化、来源和活动旧入口的兼容包装 |
| `.harness/lib/workflow.mjs` | 会议纪要等多实体操作的单层编排与事务结果聚合 |
| `.harness/lib/transaction.mjs` | 跨文件 staging、统一提交、失败回滚和写锁 |
| `.harness/lib/views.mjs` | 甘特图、活动、知识、交付物和规则生成视图 |
| `.harness/lib/lint.mjs` | 数据、生命周期、文件、Git 和模块边界的只读检查 |
| `.harness/lib/environment.mjs` | Skills、记忆、Git、软链接、文件大小和模块规模环境审计 |
| `.harness/lib/brief.mjs` | 基于近期活动、计划和待办事项生成每日引导 |
| `.harness/lib/maintain.mjs` | 单次安全重建、完整检查、规则候选与到期复查 |
| `.harness/scripts/harness.mjs` | ChatGPT 调用后台能力的统一入口 |
| `.harness/tmp/` | ChatGPT 执行本地维护时使用的临时输入 |
| `.harness/cache/` | 可重新生成的缓存 |
| `.harness/backups/` | 不进入 Git 的临时恢复材料 |

ChatGPT 会在重要工作完成后自动重建甘特图、活动、知识、交付物和规则视图，随后检查日期、状态、重复 ID、失效引用、审批范围、基线摘要、任务依赖、完成证据、交付生命周期、Wiki 生命周期、归档哈希、规则提案、生成文件漂移和 Git 跟踪范围。

`.harness/` 中每个 MJS 模块最多 500 行，这一边界由本地 lint 和测试机械检查。新增职责应进入范围清晰的小模块，`core.mjs` 只维护稳定导出，不重新堆积实现。

内部写入使用统一信封：`schema_version`、稳定 `operation_id`、`type`、`actor`、`reason`、`source_ids`、可选 `approval` 和类型化 `payload`。信封、payload 和事实记录都是封闭字段契约，拼写错误和未知字段在写入前被拒绝。输入与当前状态先通过同一契约校验，再进入临时事务目录；所有目标都准备成功后才统一替换。重复 `operation_id` 返回第一次记录的结果，不重复分配 ID 或生成活动；重复来源也会保留一次去重操作审计。

确认会议纪要等跨实体同步使用轻量 `workflow.apply`：把需求、预测/实际、风险、决定、收件箱和活动等普通类型化操作放入一次事务，任一步失败整体回滚。它不是通用编排平台，也不会代替受控变更审批；已批准基线的新增、修改和删除均需逐目标精确 before/after 的变更请求。

## `tests/` 和根目录配置

| 路径 | 作用 |
|---|---|
| `tests/core.test.mjs` | 覆盖契约、事务回滚、幂等、审批、基线、来源、活动、交付、Wiki、规则、Git 和端到端流程 |
| `.gitignore` | 排除原始归档、渲染文件、缓存、临时文件、依赖目录和凭证文件 |
| `.gitattributes` | 强制 Markdown/JSON/MJS 使用 LF，并将 Office、PDF 和图片标记为二进制 |
| `package.json` | 声明后台工具环境和自动检查入口；无需项目经理操作 |
| `AGENTS.md` | ChatGPT 每次进入项目时遵循的总工作契约和批准边界 |

## Git 中保留什么

| 类别 | 默认策略 |
|---|---|
| Harness 指令、Skills、模板和检查规则 | 保留 |
| 项目目标、需求、计划、风险、决定和状态 | 保留 |
| 结构化来源、必要引用、哈希和 Wiki | 保留 |
| 当前交付物和仍有效的批准终稿 | 保留 |
| 甘特图、知识库和交付物索引 | 保留 |
| 精简活动记录、记忆和有效规则 | 保留 |
| 原始邮件、完整聊天导出、截图和附件 | 排除 |
| 历史草稿、旧二进制和临时渲染 | 排除 |
| 缓存、备份、完整模型对话和工具日志 | 排除 |
| 凭证、密钥、Token、Cookie 和 `.env` | 排除 |

Git 对项目经理是可选且不可见的后台能力。Harness 不会擅自初始化仓库、提交、推送、清理文件或改写历史。

## 哪些变化需要你的确认

ChatGPT 可以自动执行低风险的本地整理，例如登记来源、添加证据说明、更新 Wiki、记录活动、更新预测进度、创建草稿和重建索引。

以下事项必须先展示简短的变更前后对比和影响，再由你确认：

- 项目目标、范围、成功标准或预算；
- 已批准的计划基线、承诺日期或关键里程碑；
- 已批准的需求、决定或正式验收标准；
- 已批准终稿的替换；
- 大范围删除或替换项目知识；
- Harness 有效规则的启用、修改或退役。

## 日常维护节奏

### 每天开始工作时

ChatGPT 会结合近期活动、当前计划、逾期和阻塞任务、依赖、近期里程碑、风险、待决审批、未处理来源、待交付事项及 Wiki/规则复查，给出不超过三个带原因的首要行动。

### 收到新信息时

ChatGPT 会先登记来源和证据，再更新低风险事实；可能改变需求或承诺的内容进入变更提案，不直接覆盖原记录。

### 汇报进展时

ChatGPT 会更新任务状态、预测/实际时间、进度、证据和下一步，并在活动记录中写明发生时间和结果。

### 完成重要工作后

ChatGPT 会更新相关项目记录、Wiki、记忆、计划和交付索引，重建自动生成文件并执行一致性检查，最后只汇报关键结果、时间变化、知识沉淀和仍需批准的事项。

### 复盘或出现重复问题时

ChatGPT 会先积累观察证据；达到重复阈值后形成有评价指标和复查时间的规则提案，待你批准后进入有效规则。

## Office 文件

安装 OfficeCLI 后，ChatGPT 可以读取、创建、修改和校验 DOCX、XLSX、PPTX，并在需要时生成不进入 Git 的临时预览进行视觉检查。

OfficeCLI 缺失时，ChatGPT 只会在任务确实需要 Office 文件时说明情况，不会自动安装。当前版本不承诺 PDF 输入解析能力；PDF 可作为归档来源保存，但是否能够可靠读取取决于当前环境可用工具。

## 常见问题

### 我需要自己维护这些目录吗？

不需要。目录结构是 ChatGPT 的长期工作空间。你可以检查其中的文档，但日常只需通过对话推进项目。

### 为什么同时存在 JSON 和 Markdown？

JSON 保存可检查、可关联的结构化事实；Markdown 用于阅读、讨论和正式交付。甘特图及多个索引由 JSON 自动生成，避免两个版本互相冲突。

### 可以直接修改甘特图吗？

不建议。应告诉 ChatGPT 要修改的任务、日期或依赖，由它更新 `project/schedule.json` 并重新生成 `project/gantt.md`。

### 为什么来源原件没有进入 Git？

邮件、截图、附件和旧版本通常体积较大、重复且难以比较。Harness 将原件留在本地归档，同时把必要摘要、引用、哈希、关系和索引保留在 Git 中。

### 换一台电脑后归档原件不存在怎么办？

项目事实和归档索引仍然可用，一致性检查会把原件标记为当前机器不可用，而不会把已经记录的来源事实静默删除。需要核验原件时再恢复对应文件。

### ChatGPT 会自动接受客户提出的变化吗？

不会。邮件、聊天或截图只是证据。影响目标、范围、预算、承诺日期、需求或验收的内容必须经过权威性判断和变更确认。

### 私有 Git 仓库还需要排除什么？

正常项目内容无需因为隐私被额外删减，但原始归档和旧二进制仍因体积与噪声排除。凭证、密钥、Token、Cookie、验证码和 `.env` 始终不应进入仓库。
